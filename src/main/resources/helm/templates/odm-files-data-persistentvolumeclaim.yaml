apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: odm-files-data
  name: odm-files-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
